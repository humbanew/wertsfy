import { Component_math_corebase_pi_constant } from "./constant.pi";

export const Component_math_corebase_arcsinh_method = (x: number): number => {
  return 1 / (((2 * Component_math_corebase_pi_constant * 1) / 4) / 90) * x;
};
