import { jsonSchemaBooleanSchema, } from "../../schemas/jsonSchema.schema.js";
import { CallableFunctionParameter } from "./CallableFunctionParameter.js";
export class CallableFunctionBoolean extends CallableFunctionParameter {
    type = "boolean";
    constructor(name, options = {}) {
        super(name, options);
    }
    static fromJSON(name, json) {
        const callableFunctionBooleanModel = jsonSchemaBooleanSchema.parse(json);
        const callableFunctionBoolean = new CallableFunctionBoolean(name, callableFunctionBooleanModel);
        return callableFunctionBoolean;
    }
    /**
     * Whether the given `JsonSchema` is a `JsonSchemaBoolean`.
     *
     * Note: This method does not check if the schema is valid, only if it has properties unique to boolean schemas.
     */
    static isBooleanSchema(json) {
        return json.type === "boolean";
    }
    toJSON() {
        const json = {
            ...this.toJSONBase(),
            type: this.type,
        };
        return jsonSchemaBooleanSchema.parse(json);
    }
}
//# sourceMappingURL=CallableFunctionBoolean.js.map