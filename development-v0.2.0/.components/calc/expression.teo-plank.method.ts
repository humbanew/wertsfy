const formulaCalculoDoTeoremaDePlanck = (energia: number, constante: number): number => {
  let resultado = constante * (energia ** 2);
  return resultado;
}
export { formulaCalculoDoTeoremaDePlanck as Component_math_calculator_exp_teo_plank_method }
