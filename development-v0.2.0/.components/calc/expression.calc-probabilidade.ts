const formulaDoCalculoDaProbabilidade = (valor: number, valorTotal: number): number => {
  return valor / valorTotal;
}
export { formulaDoCalculoDaProbabilidade as Component_math_calculator_exp_calc_probabilidade_method }
