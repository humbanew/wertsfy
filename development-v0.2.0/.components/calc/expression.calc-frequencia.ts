const formulaCalculoDaFrequencia = (periodo: number): number => {
  let resultado = 1 / periodo;
  return resultado;
}
export { formulaCalculoDaFrequencia as Component_math_calculator_exp_calc_frequencia_method }
